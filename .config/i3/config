# Thi file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
gaps inner 18
gaps outer 6
# smart_gaps 
# default looks
default_border pixel 2
#default_border normal
default_floating_border normal
hide_edge_borders smart
focus_follows_mouse no
popup_during_fullscreen smart
workspace_auto_back_and_forth yes
#for_window [all] title_window_icon padding 1px
title_align left
# sticky floating windows or something el!!
# for_window [instance=notepad] sticky enable | diable |toggle 
# for_window [instance="telegram-desktop"] sticky toggle


# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 12
font pango: JetBrains Mono, Noto Serif CJK SC Black 8
#font pango: .SF NS Mono, Noto Serif CJK SC Black 9
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- ~/.config/i3/lock.sh
exec --no-startup-id picom --experimental-backends --config $HOME/.config/picom.conf --log-level INFO --log-file /tmp/picom.log  
# exec --no-startup-id gnome-keyring-daemon --start 
exec --no-startup-id 1password --silent
exec_always --no-startup-id fcitx5 
# exec HDMI-1 right dell 2514p
#exec_always --no-startup-id $HOME/.screenlayout/hdmi-1-right.sh 
exec --no-startup-id $HOME/.config/i3/cw.sh ~/Images/wallpapers 120 
# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec_always --no-startup-id /usr/bin/nm-applet
exec_always --no-startup-id /usr/bin/blueman-applet
# this a gtk-clipboard manager
# exec_always --no-startup-id parcellite

exec --no-startup-id /usr/bin/chromium
exec --no-startup-id kitty
#exec --no-startup-id vlc
exec --no-startup-id dunst  
exec --no-startup-id /usr/bin/typora
# udiskie 
exec --no-startup-id udiskie -s
# pacman install  pasystray pulseaudio icon tray
# exec --no-startup-id pasystray

# xset intel_backlight 
bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 5 && $refresh_i3status && dunstify -r 1002 -u low --timeout=1500 -h int:value:`xbacklight -get` "xbacklight  "
bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 5 && $refresh_i3status &&  dunstify -r 1002 -u low --timeout=1500 -h int:value:`xbacklight -get` "xbacklight  "
# change volume or toggle mute  amixer -D sysdefault:sofhdadsp sset Capture toggle
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer sset Master 5%+ && $refresh_i3status && /home/lee/.config/dunst/audio 
bindsym XF86AudioLowerVolume exec --no-startup-id amixer  sset Master 5%- && $refresh_i3status && /home/lee/.config/dunst/audio
bindsym XF86AudioMute exec --no-startup-id amixer sset Master toggle && $refresh_i3status && /home/lee/.config/dunst/audio 
bindsym XF86AudioMicMute exec amixer -D sysdefault:sofhdadsp sset Capture toggle && $refresh_i3status && dunstify -u critical --timeout=1500 "Mic toggle"

#bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status && /home/lee/.config/dunst/audio 
#bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume  @DEFAULT_SINK@  +2% && $refresh_i3status && /home/lee/.config/dunst/audio
#bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume  @DEFAULT_SINK@  -2% && $refresh_i3status && /home/lee/.config/dunst/audio
# set mic mute/toggle 
##bindsym XF86AudioMicMute exec amixer sset Capture toggle && $refresh_i3status && dunstify -u critical --timeout=1500 "Mic toggle"
#bindsym XF86AudioMicMute exec pactl set-source-mute  @DEFAULT_SOURCE@  toggle && $refresh_i3status && dunstify -u critical --timeout=1500 "Mic toggle"
# screenshot use scrot	
bindsym --release mod1+a exec "scrot '%Y-%m-%d-%H-%M-%S_$wx$h.png' -e 'mv $f ~/Images/shots/' && dunstify -u --timeout=2500 'Scrot' `ls -t $HOME/Images/shots |head -n 1` "
# screenshot select 
bindsym --release mod1+z exec "scrot -l style=dash,width=3,color='red' -s -f '%Y-%m-%d-%H-%M-%S_$wx$h.png' -e 'mv $f ~/Images/shots/' && dunstify -u --timeout=2500 'Scrot' `ls -t $HOME/Images/shots |head -n 1` "

# mouse bind  

# thinkpad x1 hotkeys 

# bindsym XF86AudioDisplay
# bindsym XF86Favorites

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
#set $refresh_i3status killall -HUP conky
#set $refresh_i3status pkill -RTMIN+1 goblocks

#set $refresh_i3blocks killall -SIGUSR1 i3blocks
#set $refresh_i3blocks killall -HUP conky

#bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
#bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
#bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
#bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod


# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
#bindsym $mod+Return exec urxvtc
bindsym $mod+Return exec kitty

# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec --no-startup-id dmenu_run

# A more modern dmenu replacement is rofi:
bindcode $mod+40 exec "rofi -modi drun,ssh -show combi"

# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# for_window [class="urxvt"] floating enable
for_window [class="^Blueman"] floating enable, border normal, focus
for_window [class="Arandr"] floating enable, border normal, focus
for_window [class="Typora"] floating enable, border normal, focus
for_window [class="^Pcsx2$"] floating enable, border normal, move position center, focus, 
for_window [instance="kbd-layout-viewer"] floating enable, resize set 1600px 900px, border normal, move position center, focus
for_window [instance="qv4l2"] floating enable, resize set 1280px 720px, border normal, move position center, focus
# for_window [class="^*$"] floating enable, move position center
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_type="dialog"] floating enable, move position center
#for_window [window_type="dialog"] floating enable, move position center
#for_window [class="URxvt"] border pixel 2
for_window [class="kitty" workspace="6"] floating enable, border normal, resize set 960px 600px, move position center, focus
#for_window [class="URxvt" workspace="6"] floating enable, border normal
for_window [class="kitty" workspace="2"] floating enable, border normal, resize set 960px 600px, move position center, focus
#for_window [class="URxvt" workspace="2"] floating enable, border normal
# for_window [class="URxvt"] floating enable
for_window [class="fcitx5-config-qt"] floating enable, border normal
for_window [class="Lxappearance"] floating enable, border normal
for_window [class="Org.gnome.Nautilus"] floating enable, resize set 1440px 900px, move position center, border normal
for_window [instance="qv4l2"] floating enable
for_window [class="feh"] floating enable, resize set 1280px 960px, move position center, border normal

for_window [instance="evince"] floating enable
for_window [class="TelegramDesktop"] floating enable, border normal,move position center
# !!!for_window [instance="ghostwriter" window_type="splash"] floating enable,resize set 1920px 1080px, border normal, move position center
#for_window [instance="marker"] floating enable, resize set 1920px 1080px, border none, move position center
for_window [class="^Typora"] floating enable, resize set 90ppt 90ppt, border none, move position center
#for_window [class="^Marker" title="Preferences"] floating enable, resize set 800px 460px, border normal, move position center, focus
#for_window [class="^Marker" title="Save As"] floating enable, resize set 800px 600px, border normal, move position center, focus
#for_window [class="^Marker" title="Export"] floating enable, resize set 800px 600px, border normal, move position center, focus
#for_window [class="^Marker" title="打印"] floating enable, resize set 800px 600px, border normal, move position center, focus
#for_window [class="^Marker" title="Open"] floating enable, resize set 800px 600px, border normal, move position center, focus

for_window [class="Seahorse"] floating enable, move position center
for_window [class="1Password"] floating enable, move position center, resize set 1440px 960px, border normal
for_window [class="SimpleScreenRecorder"] floating enable
for_window [class="mpv"] floating enable, move position center, focus
for_window [class="vlc"] border normal, move position center, focus

#for_window [title="主题"] floating enable, resize set 800px 600px, border normal, move position center
for_window [instance="virt-manager"] floating enable, border normal
for_window [instance="nm-connection-editor"] floating enable, border normal
for_window [class="Chromium"] border pixel 0
for_window [class="baidunetdisk"] floating enable, resize set 1400px 900px, move position center 

#for_window [class="Subl"] move to workspace 6
for_window [class="Remote-viewer"] floating enable
for_window [class="Wireshark" title="^Wireshark · "] floating enable, move position center 

#for_window [instance="pavucontrol"] floating enable, resize set 1023px 950px, move to position center

#for_window [class="qpaeq"] floating enable, move position center

#for_window [class="(?i)kitty"] title_format "<span foreground='white'>%title</span>"
for_window [all] title_format "<span foreground='white'>%title</span>"

# for_window [class="Chromium"] floating 
# resizing containers/windows 
# resize grow|shrink <direction>
# resize set width
# resize set height
# resize set width px height px // ppt
# for_window [class=" "] resize set 640 480


# assign chromium to workspace 2
assign [class="Chromium"] 2
#assign [instance="marker"] 3
assign [class="vlc"] 4
assign [class="mpv"] 4
#assign [class="openshot"] → 5
assign [class="obs"] → 6
assign [class="Typora"] → 3
assign [class="^Pcsx2$"] → 10

# scratchpad
bindsym mod1+shift+s move scratchpad
bindsym mod1+s scratchpad show
# move to scratchpad
bindsym $mod+x [class="1Password"] move scratchpad
# toggle floating window
bindsym $mod+t floating toggle
# i3lock ~~
#set $Locker i3lock-fancy -pgf JetBrains-Mono-Regular -t 'Please Type Password Here' -n -- maim -q && sleep 1
set $Locker .config/i3/lock.sh && sleep 1

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $Locker && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"  

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym mod1+L mode "$mode_system"


# set workspace 8 as hdmi-1 output
workspace 1 output eDP-1
workspace 2 output eDP-1
workspace 3 output eDP-1
workspace 4 output eDP-1
workspace 5 output eDP-1
workspace 6 output eDP-1
workspace 7 output eDP-1
# hdmi-1 ws on 8
workspace 8 output HDMI-1
workspace 9 output eDP-1
workspace 10 output eDP-1
# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart


# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -f 'xft:JetBrains Mono 11' -m 'Exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"



# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"


# colors for windows 

# class                 border     backgr     text      indicator    child_border
client.focused          #4b00a980  #4b00a980  #00ff00   #4b00a980      #4b00a990
client.focused_inactive #70809080  #70809080  #ffffff   #70809080      #70809080
client.unfocused        #333333    #22222280  #888888   #292d2e      #222222
client.urgent           #2f343a    #90000080  #ffffff   #900000      #900000
client.placeholder      #000000    #0c0c0c80  #ffffff   #000000      #0c0c0c

#client.background       #ffffff
client.background       #20202080




# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
     output eDP-1
     position top
     id bar-0
     mode dock
     status_command i3status
#    status_command ~/.config/i3/conky.sh
     i3bar_command i3bar --transparency
     tray_output eDP-1
#    tray_padding 3 
#    font pango:Font Awesome 6 Free Solid 8, JetBrains Mono NL ExtraBold 8
     font pango:Font Awesome 8, JetBrains Mono NL 8
#    font pango:Font Awesome 8, .SF NS Mono 8
#    font pango:Font Awesome 8, Meslo LG S Bold 8
     colors {
        background #00000085
        statusline #ffffff
        separator  #ffff00
#	<colorclass>       <border>  <background>   <text>
 	focused_workspace  #98fb98    #556b2f90     #00ff00
        active_workspace   #333333    #5f676a80     #ffffff
        inactive_workspace #333333    #222222       #888888
        urgent_workspace   #2f343a    #900000       #ffffff
        binding_mode       #2f343a    #900000       #ffffff
    }
}

#bar {
#	output HDMI-1
#	position top
#	mode dock
#	tray_output HDMI-1
#        i3bar_command i3bar --transparency
# 	colors {
#        	background #20202080
#        	statusline #ffffff
#        	separator  #ffffff
#		<colorclass>       <border>  <background>   <text>
# 		focused_workspace  #98fb98    #556b2f90     #00ff00
#       	active_workspace   #333333    #5f676a80     #ffffff
#        	inactive_workspace #333333    #222222       #888888
#        	urgent_workspace   #2f343a    #900000       #ffffff
#      	 	binding_mode       #2f343a    #900000       #ffffff
#    	}
#
#}
